import dat from "dat.gui";

// 创建一个GUI对象
const matFolder = new dat.GUI();
// 修改gui界面默认的style属性
matFolder.domElement.style.right = '0px'
matFolder.domElement.style.width = '500px'

 export default matFolder;